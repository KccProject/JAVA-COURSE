public class java_datatypes {
    public static void main(String args[]){
        //byte
        byte b = 8;
        System.out.println(b);
        char ch = 'a';
        System.out.println(ch);
        boolean v = false;
        double p = 10.8;
        // for storing very big floating number than we use 'double' instead of 'float'
        int n = 56;
        // for storing very big integer number than we use 'long' instead of 'int'
    }
}
